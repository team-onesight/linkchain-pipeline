ansible_user: ubuntu
ansible_ssh_private_key_file: private_key.pem

ansible_ssh_common_args: >-
  -o ProxyCommand="ssh -o StrictHostKeyChecking=no -W %h:%p -q {{ ansible_user }}@{{ bastion_host }} -i {{ ansible_ssh_private_key_file }}"
  -o StrictHostKeyChecking=no
