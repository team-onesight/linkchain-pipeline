[project]
name = "linkchain-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
]

[dependency-groups]

dev = [
    "apache-airflow==3.1.2",
    "pytest>=9.0.2",
    "pytest-mock>=3.15.1",
]
